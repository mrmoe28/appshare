# Environment Variables (Optional)
# Vercel automatically configures Blob storage, so no env vars are required!

# If you want to add custom configuration in the future:
# CUSTOM_DOMAIN=https://your-domain.com
# MAX_FILE_SIZE=104857600  # 100MB in bytes